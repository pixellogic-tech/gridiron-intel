import{useState}from'react';import{useNavigate}from'react-router-dom';import{useAuth}from'../contexts/AuthContext';import{Button}from'../components/ui/Button';export default function Login(){const{login}=useAuth();const n=useNavigate();const[e,sE]=useState('');const[p,sP]=useState('');const h=async(ev:React.FormEvent)=>{ev.preventDefault();await login(e,p);n('/coach/dashboard')};return<div style={{minHeight:'100vh',background:'linear-gradient(135deg,#0f172a,#1e3a8a)',display:'flex',alignItems:'center',justifyContent:'center'}}><div style={{background:'white',padding:'2rem',borderRadius:12,width:400}}><h2 style={{textAlign:'center',marginBottom:'2rem'}}>Login</h2><form onSubmit={h}><input type="email" placeholder="Email" value={e} onChange={ev=>sE(ev.target.value)} style={{width:'100%',padding:'.75rem',marginBottom:'1rem',borderRadius:8,border:'1px solid #ccc'}}/><input type="password" placeholder="Password" value={p} onChange={ev=>sP(ev.target.value)} style={{width:'100%',padding:'.75rem',marginBottom:'1.5rem',borderRadius:8,border:'1px solid #ccc'}}/><Button type="submit" v="primary" style={{width:'100%'}}>Login</Button></form></div></div>}
